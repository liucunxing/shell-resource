# V1.4 前端验证记录

## 2026-09-23 API 联调

默认 API 模式已完成本地 FastAPI + SQLite 浏览器联调。`npm test` 为 11 个文件、39 项通过；生产构建通过。草稿刷新恢复、同步快照隔离、两类真实 Excel 往返、Owner 隔离、负责人只读、管理员日志和管理层预览均已实测。

本次完整证据、外部验收边界见 [本地验收记录](../docs/quickwin-validation.md)。真实 PostgreSQL 与百炼尚未验收；用户已确认先用本地。

## 以下为 V1.3 独立演示模式的历史验证
验证日期：2026-09-22。范围为 `frontend/`；使用独立浏览器会话与同源演示数据。后端未修改，未调用真实问数或业务接口。测试产生的 Excel、脚本、截图位于 Git 忽略的 `output/`。

## 自动检查

- `npm test`：7 个文件、28 项测试通过。其中 6 项为迁入的 V1.3 复合回归套件，覆盖引擎、持续同步、找平、历史与原始数据、Owner Excel、管理员 Excel；另 22 项为 iframe URL 校验。
- `npm run build`：TypeScript 入口检查与 Vite 生产构建通过。迁入 JSX / JS 使用 allowJs，未开启 checkJs，不将本次构建描述为全部业务代码的静态类型检查。
- ExcelJS 仍有超过 500KB 的分包提示，按需加载；首次页面不加载 ExcelJS。
- `git diff --check`：通过。
- 旧六步工作流的代码及测试已移除，当前验收对应 V1.3；未复用旧版 38 项测试的通过数字。

## 核心流程（实际生产预览）

| 场景 | 验证结果 |
| --- | --- |
| 非法金额 | 输入 1.234 时阻止切页与切换角色，并保留原始输入便于修正 |
| 一键找平 | I-005 差额 18,500 进入专用自动其他预算行，经销商金额不变 |
| 持续同步 | I-005 同步后仍可编辑；第二次同步保留两条独立历史 |
| 草稿隔离 | 修改经销商金额但未同步，管理层金额及六点 Insight 文本均不变；再次同步后管理层更新 |
| 刷新保存 | 经销商 100 改为 47,400 后刷新与重开 Initiative，金额保留 |
| Owner 范围 | 首页与 Insight 选择范围仅含本人 Initiative；跨角色页面重置 |
| 六点 Insight | 生成六张分析卡；保存提示词后旧分析变为待更新；重新生成后状态恢复；切换 Initiative 后通用入口跟随当前项 |
| 历史参考 | 经销商 100 / 200 的 2025 整体 Yield 分别为 6.77 / 8.25；2026 保留累计口径提示 |
| 问数模拟 | 历史口径回答、上下文、对话记录恢复均可用；提问不改变业务存储 |
| 会话清理 | 折叠保留输入草稿；切换演示场景清空旧问数会话 |

根流程脚本：`output/playwright/v13-business-check.js`、`v13-final-check.js`、`v13-insight-reference-check.js`。执行期间无页面运行错误。

## Excel 浏览器往返

Owner：真实下载 I-005 模板，用 ExcelJS 将经销商 100 的金额由 47,500 改为 47,623.45，并添加 100 的新增经销商预留及说明。预览显示安排总额 351,723.45、差额 18,276.55。取消后本机存储字节级不变；确认后正确写入，刷新保留。导入不创建同步版本。篡改 Initiative 编号或使用公式金额时显示错误、禁止确认，存储不变。

管理员：真实下载 75 项配置，修改 I-001 预算 180,000 → 180,123.45、Owner ICE-1 → ICE-2。预览 1 项变化、74 项不变；取消不写入。确认与刷新后变更保留；75 项的经销商、其他预算和同步快照均未变化，其余 74 项完整对象未变化。原因新增、改名、停用及指引 v1 → v2 均可保存。弹窗 Escape 关闭并归还焦点。两类 Excel 浏览器验证均无控制台错误或警告。

证据：`output/playwright/v13-owner-*`、`v13-admin-*`，包括管理员明细验证记录。

## 布局与辅助面板

- 1440px 检查 Owner、负责人和管理层；1024px 检查同三类角色及参考、Insight 面板；390px 检查 Owner、负责人、管理层和管理员，未出现整页横向溢出。宽表在容器内部滚动。
- 修复首页 Initiative 表格只占半列的问题，1440px 验证表格占面板宽度超过 95%。
- 1440px 下问数面板从 272px 导航右缘展开；390px 下覆盖全部可用内容宽度。可恢复紧凑侧栏。
- 导航和问数面板支持 Escape 关闭及焦点返回。手机菜单和工作面板互斥。
- 未配置 URL 时 iframe 为 0，本地问数明确标注为模拟。
- 已人工查看桌面首页、编辑与管理层，以及手机首页、管理员和问数截图。

最新截图为 `output/playwright/v13-final-*.png`。历史角色宽度检查为 `output/v13-audit-*.png`。

## iframe 本地夹具验证

使用独立 Vite 开发端口 5174 配置本地跨源夹具 4175，无真实易问请求。首次打开问数前 iframe 与夹具加载次数均为 0；打开后为 1。切换参考工具、折叠重开、部门与首页导航均保留同一实例及输入，加载次数不增加。手动重新加载产生新实例并清空输入；切换角色先卸载，重新打开再生成新实例。控制台无错误或警告。测试中一次代码热更新触发整页重载，待更新稳定后重新验证切页保持行为并通过。测试专用服务已停止。

## 边界

执行追踪为原型预留入口。认证、后端授权、并发、真实数据、模型分析和易问服务登录仍需后续集成。此次验证支持独立前端演示交付，不表示这些外部能力已验收。

## 2026-09-23 三栏工作台调整

本节覆盖上一版“辅助栏展开到导航右缘”的布局规则。桌面宽度 ≥1024px 时，参考、反馈、Insight、问数统一作为右侧独立列，宽度 320–380px；左导航、中间工作台保持可见，中间工作台不再设为 inert。移除最大化按钮。窄屏继续使用遮罩抽屉。

- 生产预览在 1920、1440、1280、1024px 下逐一展开四个工具，共 16 组几何检查通过：三列不重叠、无整页横向溢出、中间区域可交互。
- 在辅助栏打开时进入 Initiative 编辑、返回列表和切换部门页面，辅助栏持续保留。
- 问数输入草稿在切换工具、切页、折叠和重开后保留；iframe 的保持挂载逻辑未改变。
- 收起辅助栏后，中间工作区恢复宽度；900、390px 下遮罩和 Escape 收起行为通过。
- 检查期间无页面运行错误。已查看 1440px 问数/参考编辑页、1024px 参考页面截图，并对窄桌面的指标字号和筛选换行作适配。
- `npm run build` 与 `npm test`（28 项）通过；ExcelJS 延迟分包提示保持原状。

本地检查脚本：`output/playwright/three-column-check.js`；截图：`output/playwright/three-column-*.png`。
- 补充修复页面初始化时立即调整窗口宽度可能保留旧抽屉状态的问题：媒体查询订阅后立即同步一次当前宽度，防止桌面已显示三栏但背景仍被禁用。
